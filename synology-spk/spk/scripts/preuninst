#!/bin/sh

PKG_NAME="${SYNOPKG_PKGNAME:-RokuDSVideoTools}"
PKG_SCRIPT_DIR="/var/packages/$PKG_NAME/scripts"

if [ -x "$PKG_SCRIPT_DIR/start-stop-status" ]; then
  "$PKG_SCRIPT_DIR/start-stop-status" stop >/dev/null 2>&1 || true
fi

exit 0
